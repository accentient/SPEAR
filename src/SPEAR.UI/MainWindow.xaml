<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SPEAR: Search Platform for Email Archive Retrieval" Height="960" Width="1600">
    <Window.Resources>
        <Style x:Key="SingleLineCellStyle" TargetType="TextBlock">
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="HorizontalGridLinesBrush" Value="LightGray"/>
            <Setter Property="VerticalGridLinesBrush" Value="LightGray"/>
        </Style>

        <Style x:Key="CenteredHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <DataTemplate x:Key="PaperclipHeaderTemplate">
            <Grid>
                <Image Source="pack://application:,,,/UI;component/paperclip.png"
                       Width="16" Height="16"
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search controls -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBox x:Name="SearchTextBox" Width="300" Height="28" Margin="0,0,10,0" VerticalAlignment="Center" VerticalContentAlignment="Center" Text="warmbronn" />
            <Button x:Name="SearchButton" Content="Search" Width="100" Height="28" VerticalAlignment="Center" IsDefault="True" />
        </StackPanel>

        <!-- SQL Query Label -->
        <Label x:Name="QueryLabel" Grid.Row="1" Margin="0,0,0,10" FontSize="12" Foreground="Gray" Padding="0" VerticalContentAlignment="Center" />

        <!-- Email grid -->
        <DataGrid x:Name="EmailDataGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  RowHeight="22" HeadersVisibility="Column" CanUserResizeRows="False"
                  VerticalAlignment="Top">
            <DataGrid.Columns>
                <DataGridTextColumn Header="EmailID" Binding="{Binding EmailID}" Visibility="Collapsed"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="130"/>
                <DataGridTextColumn Header="Sender" Binding="{Binding Sender}" Width="300"/>
                <DataGridTextColumn Header="To" Binding="{Binding To}" Width="300"/>
                <DataGridTextColumn Header="Subject" Binding="{Binding Subject}" Width="225"/>
                <DataGridTextColumn Header="Body" Binding="{Binding BodySnippet}" Width="*" ElementStyle="{StaticResource SingleLineCellStyle}"/>
                <DataGridTemplateColumn Width="40"
                                         HeaderTemplate="{StaticResource PaperclipHeaderTemplate}"
                                         HeaderStyle="{StaticResource CenteredHeaderStyle}"
                                         SortMemberPath="AttachmentCount">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding AttachmentCount}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
